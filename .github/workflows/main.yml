


name: container pulling node version
on: push
jobs:
    first-job:
        name: run node version from docker
        runs-on: ubuntu-latest
        container: node:14
        steps:
            - run: node --version
              name: check node version
            - run: npm --version
              name: check node version




# on: push
# jobs:
#     first-job:
#         runs-on: ubuntu-latest
#         container: node:14
#         steps:
#             - run: node --version
#             - run: npm --version




# on: push
# jobs:
#     first-job:
#         runs-on: ubuntu-latest
#         steps:
#             - run: node --version
#             - run: npm --version
       



# on: push
# jobs:p
#     first-job:
#         runs-on: windows-latest
#         steps:
#             - run: git --version