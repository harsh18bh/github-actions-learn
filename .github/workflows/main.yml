on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        container: node:14
        steps:
            - run: node --version
            - run: npm --version

# on: push
# jobs:
#     first-job:
#         runs-on: ubuntu-latest
#         steps:
#             - run: node --version
#             - run: npm --version
       



# on: push
# jobs:p
#     first-job:
#         runs-on: windows-latest
#         steps:
#             - run: git --version